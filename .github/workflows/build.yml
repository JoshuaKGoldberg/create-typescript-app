name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    uses: [actions/checkout@v3, ./.github/actions/prepare]
    steps:
      - id: test
        run: |
          pnpm build
          node ./src/index.js
