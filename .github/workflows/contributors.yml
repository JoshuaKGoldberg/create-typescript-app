jobs:
  contributors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: ./.github/actions/prepare
      - uses: JoshuaKGoldberg/all-contributors-auto-action@v0.2.0

name: Contributors

on:
  push:
    branches:
      - main
